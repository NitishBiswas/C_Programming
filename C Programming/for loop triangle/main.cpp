//using for loop for triangle
#include <stdio.h>
main()
{
   int i,j,rows,k;
   printf("Input number of rows : ");
   scanf("%d",&rows);
   for(i=1;i<=rows;i++)
   {
	for(j=1;j<=i;j++)
	   printf(" N ",k);
	printf("\n");
   }
}
